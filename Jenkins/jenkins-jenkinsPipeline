pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {
                if ( Hello_World == True ){
                    echo 'Hello World'
                    }
                else{
                    echo 'Skipping'
                }
            }
        }
        stage('Install Dependencies') {
            steps {
                sh 'pip3 install pytest'
            }
        }
        stage('Running Script') {
            steps {
                script {
                    if ( Pytest == True ){
                        sh 'pytest code1.py -v'                        }
                    else{
                        echo 'Skipping'
                    }

            }
        }
    }

    post {
        always {
            echo 'Script Done!'
        }

        success {
            echo 'Script Success'
        }

        failure {
            echo 'Script Failure'
        }
    }
}
