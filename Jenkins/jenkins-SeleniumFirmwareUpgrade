
// Functions

def archiveArtifacts(pattern) {
    step([$class: 'ArtifactArchiver', artifacts: pattern, fingerprint: true, onlyIfSuccessful: false])
}


// Main Pipeline

pipeline {
    agent { node { label "${TARGET_STAND}"} }

    stages {
        stage('Prepare tests') {
            steps {
                script {
                    echo "----------------------------------------"
                    echo "--- TARGET STAND: ${TARGET_STAND}"
                    echo "----------------------------------------"
      	        }
      	    }
        }
        stage('Install Python Dependencies') {
            steps {
                script {
                    echo "----------------------------------------"
                    echo "Installing Dependencies"
                    echo "----------------------------------------"
                    sh "sh utilities/requirements.txt"
                }
            }
        }
        stage('Download Firmware'){
            steps {
                script {
                    echo "----------------------------------------"
                    echo "Downloading the Build"
                    echo "----------------------------------------"
                    sh "mkdir -p artifacts"
                    sh "pwd"
                    // Ensure you use the correct parameter name from your Jenkins job
                    def customBuildFile = params.UPLOADED_FIRMWARE
                    // Define the log file path once
                    def logFile = "${workspace}/artifacts/flashing-device.log"

                    // This parsing logic is correct and working.
                    def url = params['Build']
                    int bonsaiIndex = url.indexOf('/bonsai/')
                    if (bonsaiIndex == -1) {
                        error "Invalid Build URL: ${url}. URL must contain '/bonsai/'."
                    }
                    def buildPath = url.substring(bonsaiIndex + 8)
                    def parts = buildPath.split('/')
                    def PLATFORM_ID = parts[0]
                    if (PLATFORM_ID == null || PLATFORM_ID.trim() == "") {
                        error "Could not parse PLATFORM_ID from URL: ${url}"
                    }
                    def platformPrefix = PLATFORM_ID.substring(0, 2)
                    def TARGET_IMG_FILENAME = "nor-ipq${platformPrefix}xx-apps.img.enc"
                    def WGET_URL = "https://jenkins.senao.com/bonsai/${buildPath}"
                    if (!WGET_URL.endsWith('/')) {
                        WGET_URL += '/'
                    }

                    echo "Parsed PLATFORM_ID: ${PLATFORM_ID}"
                    echo "Target Image Filename: ${TARGET_IMG_FILENAME}"
                    echo "Full WGET URL: ${WGET_URL}"

                    // Check if a custom build was uploaded
                    if (customBuildFile) {
                        // --- 1. A FILE WAS UPLOADED ---

                        sh "echo 'Using uploaded custom build: ${customBuildFile}' | tee \"${logFile}\""
                        sh "mv -f '${customBuildFile}' ${TARGET_IMG_FILENAME} 2>&1 | tee -a \"${logFile}\""

                        sh "echo 'Downloading -apps.tgz file...' | tee -a \"${logFile}\""
                        sh "wget --no-verbose --no-parent --no-check-certificate --recursive --level=1 -A '*-apps.tgz' --no-directories ${WGET_URL} 2>&1 | tee -a \"${logFile}\""
                        sh "mv -f *-apps.tgz test_firwmare.tgz 2>&1 | tee -a \"${logFile}\""

                    } else {
                        // --- 2. NO FILE UPLOADED, DOWNLOAD FROM URL ---

                        sh "echo 'No custom build found. Downloading from URL.' | tee \"${logFile}\""

                        // --- CHANGED ---
                        // Added *both* image patterns to the -A flag:
                        // your original '*-B*[0-9]-enc.img' AND your new '*-apps.img.enc'
                        sh "wget --no-verbose --no-parent --no-check-certificate --recursive --level=1 -A '*-B*[0-9]-enc.img,*-apps.img.enc,*-apps.tgz' --no-directories ${WGET_URL} 2>&1 | tee -a \"${logFile}\""
                        // --- END OF CHANGE ---

                        // Rename the downloaded .tgz file
                        sh "mv -f *-apps.tgz test_firwmare.tgz 2>&1 | tee -a \"${logFile}\""

                        // --- CHANGED ---
                        // This shell script now checks for *both* patterns,
                        // finds whichever one was downloaded, and renames it.
                        sh """
                           #!/bin/bash
                           imgfile=""

                           # First, check for the original pattern
                           imgfile=\$(ls *-B*[0-9]-enc.img 2>/dev/null | head -n 1)

                           # If that's not found, check for the new pattern
                           if [ -z "\$imgfile" ]; then
                               imgfile=\$(ls *-apps.img.enc 2>/dev/null | head -n 1)
                           fi

                           # Now, rename whichever one was found
                           if [ -n "\$imgfile" ]; then
                               echo "Found image file: \$imgfile. Renaming to ${TARGET_IMG_FILENAME}"
                               mv -f "\$imgfile" "${TARGET_IMG_FILENAME}"
                           else
                               echo "ERROR: No downloaded .img file matching '*-B*[0-9]-enc.img' OR '*-apps.img.enc' was found."
                               exit 1
                           fi
                        """ + " 2>&1 | tee -a \"${logFile}\""
                        // --- END OF CHANGE ---
                    }

                    // Archive the final .img file, regardless of its source
                    archiveArtifacts(TARGET_IMG_FILENAME)
                }
            }
        }
        stage('Firmware Upgrade') {
            steps {
                script {

                    def localIP = params['Local IP Address']
                    def remoteIP = params['Remote IP Address']
                    def model = params['Model']
                    def buildURL = params['Build']
                    def single = params['Single Image']

                    echo "Local IP Address: ${localIP}"
                    echo "Remote IP Address: ${remoteIP}"

                    echo "----------------------------------------"
                    echo "Upgrading the Firmware"
                    echo "----------------------------------------"
                    sh "mkdir -p artifacts"
                    //for (int i = 1; i <= FIRMWARE_UPGRADE_COUNT.toInteger() ; i++) {
                    //    sh "pytest -v -s testCases/test_firmwareUpgrade.py::test_Upgrade 2>&1 | tee ${workspace}/artifacts/firmware-upgrade-device.log"
                    //}
                    archiveArtifacts('artifacts/**/*')
                    //archiveArtifacts('SerialLogs_Device.log')
                }
            }
        }
    }
    post {
        always {
            echo "-----------------------------------------"
            echo 'Script Done!'
            echo "-----------------------------------------"
        }

        success {
            echo "-----------------------------------------"
            echo 'Script Success'
            echo "-----------------------------------------"
        }

        failure {
            echo "-----------------------------------------"
            echo 'Script Failure'
            echo "-----------------------------------------"
        }
    }
}